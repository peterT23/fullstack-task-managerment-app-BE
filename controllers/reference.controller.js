const Reference = require("../models/ReferenceDocument");
const { sendResponse, AppError, catchAsync } = require("../helpers/utils");

const referenceController = {};
//register user

module.exports = referenceController;
